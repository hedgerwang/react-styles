/**
 * @providesModule ExampleStyleRules
 */

var ReactStyle = require('ReactStyle');

var ExampleStyleRules = ReactStyle.create({
  '.body': {
    color: '#666',
    fontFamily: 'Helvetica',
    fontSize: '12x',
  },
});

module.exports = ExampleStyleRules;
